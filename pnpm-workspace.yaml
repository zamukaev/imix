packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  # prisma query engine binaries
  '@prisma/engines': true
  prisma: true
  # next/image optimisation
  sharp: true
  # native resolver used by eslint-config-next
  unrs-resolver: true
